<!DOCTYPE html>
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    meta(http-equiv="X-UA-Compatible", content="ie=edge")

    link(rel="stylesheet", href="/css/styles.css")
    link(rel="stylesheet", href="https://fonts.googleapis.com/css?family=Lato:300,400,700&display=swap")

    if title
      title Express Blog - #{title}
    else
      title Express Blog

  body
    .navigation
      nav.navigation__nav
        span.navigation__brand Express Blog
        ul.navigation__list
          li.navigation__item
            a.navigation__link(href="/") Home
          li.navigation__item
            a.navigation__link(href="/create") New Article

          li.navigation__vl &nbsp;
          li.navigation__item
            a.navigation__link(href="/auth/login") Login
          li.navigation__item
            a.navigation__link(href="/auth/signup") Sign up


    .messages
      if messages.info
        span.message.message--info #{messages.info} #[span.message__close &Cross;]

      if messages.success
        span.message.message--info #{messages.success} #[span.message__close &Cross;]

      if messages.error != '' && messages.error
        span.message.message--error #{messages.error} #[span.message__close &Cross;]

    main.main
      block body
    
    footer.footer
      p.footer__text
        | Copyright 2019 &copy;&nbsp;
        a(href="https://github.com/habiiev" target="_blank").footer__link Oleksii Habiiev
    
    script(src="/js/script.js")